-- Need to set this to avoid parallelization leading to a file permissions error. 
SET max_parallel_workers = 0;
SET max_parallel_workers_per_gather = 0;

SELECT *
FROM public.green_taxi
ORDER BY trip_distance DESC
LIMIT 100