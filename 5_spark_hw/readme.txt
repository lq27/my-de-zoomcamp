----------------------
HW 5: Batch processing
----------------------
1. 3.3.2

To get into spark shell (assuming you have saved the paths to spark and hadoop as environment variables):

$ spark-shell
scala> spark.version

The first command is from bash ($) and the second is inside the spark shell (scala>)
The second command returns: res0: String = 3.3.2


2. 
